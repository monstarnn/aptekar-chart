apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: aptekar-api
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: aptekar
        component: aptekar-api
    spec:
      containers:
      - name: aptekar-api
        image: {{ .Values.api.image }}
        readinessProbe:
          httpGet:
            path: /probe
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          successThreshold: 1
          failureThreshold: 3
        imagePullPolicy: {{ default "" .Values.api.imagePullPolicy | quote }}
        env:
        - name: PG_HOST
          value: {{ default "localhost" .Values.api.postgres.host | quote }}
        - name: PG_PORT
          value: {{ default "5432" .Values.api.postgres.port | quote }}
        - name: PG_USER
          value: {{ default "postgres" .Values.api.postgres.user | quote }}
        - name: PG_PWD
          value: {{ default "" .Values.api.postgres.password | quote }}
        - name: FAKE_PREDICTED
          value: {{ default "false" .Values.api.predicted.fake | quote }}
        - name: AMIRA_HOST
          value: {{ default "" .Values.api.predicted.amiraHost | quote }}
        ports:
        - name: http
          containerPort: 8082
        resources:
{{ toYaml .Values.api.resources | indent 10 }}
